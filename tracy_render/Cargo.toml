[package]
name = "tracy_render"
version = "0.1.0"
edition = "2024"

[dependencies]
num-traits = "0.2.19"
rand = "0.9.2"
tracy_math = { version = "0.1.0", path = "../tracy_math" }
tracy_scene = { version = "0.1.0", path = "../tracy_scene" }
