[package]
name = "tracy_export"
version = "0.1.0"
edition = "2024"

[dependencies]
tracy_macros = { version = "0.1.0", path = "../tracy_macros" }
tracy_render = { version = "0.1.0", path = "../tracy_render" }
